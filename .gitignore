/SonicPlayer.xcodeproj/project.xcworkspace/xcuserdata/hsawaed.xcuserdatad/UserInterfaceState.xcuserstate
/.DS_Store
.DS_Store
/SonicPlayer.xcodeproj/xcuserdata/hsawaed.xcuserdatad/xcschemes/xcschememanagement.plist
xcschememanagement.plist

# Xcode per-user state — never shared
xcuserdata/
*.xcuserstate

# Superpowers brainstorming scratch
.superpowers/

# Screenshots, mocks and scratch captures. Some are tracked deliberately (27 files
# predate this); new ones are NOT swept in by a stray `git add -A` — that happened
# twice on #112 and cost 60 MB each time. Add intentional ones with `git add -f`.
artifacts/
